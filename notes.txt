npm run dev // en carpte mi-proyecto front
dotnet run // en api back
antlr4 -Dlanguage=CSharp -o analyzer -package analyzer -visitor -no-listener ./grammars/*.g4



Accioens Importantes
Slices
Func main
Nil nullo arreglar
Switch CAse
for slice
Verificar tipos en parametros y cantidad
Verificar tipos en asignacion
Reportes
slice len
Slice strings.Join append


type Persona struct {
    int altura
     
    func constructor(nuevaAltura int){
        this.altura = nuevaAltura
    }
}

var yo = new Persona(30)
fmt.Println(yo.altura)